version: 3
automerge: true
projects:
  - name: ecr/zerone-id/ap-northeast-2
    dir: terraform/ecr/zerone-id/ap-northeast-2
    workspace: default
    terraform_version: 1.5.7
    autoplan:
      when_modified: [
         "*.tf"
      ]
      enabled: true
    apply_requirements: []
    workflow: id


#### Workflows #####
workflows:
  # id
  id:
    plan:
      steps:
        - init:
            extra_args: [
              '-backend-config="role_arn=arn:aws:iam::066346343248:role/atlantis-demo-id-admin"',
              '-upgrade=false'
            ]
        - plan:
            extra_args: [
              "-var 'assume_role_arn=arn:aws:iam::066346343248:role/atlantis-demo-id-admin'"
            ]
    apply:
      steps:
        - apply

